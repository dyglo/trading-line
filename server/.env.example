DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
JWT_ACCESS_SECRET="replace-with-32-character-random-string"
JWT_REFRESH_SECRET="replace-with-32-character-random-string"
ACCESS_TOKEN_EXPIRES_IN=900
REFRESH_TOKEN_EXPIRES_IN=604800
# Comma-separated list of allowed origins (set to * to reflect incoming origins)
CORS_ORIGIN="http://localhost:8080,https://your-production-domain.vercel.app"
COOKIE_SECURE=true
# Uncomment and set if you need to scope cookies to a specific domain
# COOKIE_DOMAIN=your-production-domain.vercel.app
